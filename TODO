?	# FIXME: maybe rename it to LIBSVM_internal??
?  could have that user can specify a different wrapper name and let search for it.
? timeout under windows.
	

	# remove old
	x$trainBinaryPath = NULL
	x$testBinaryPath = NULL

	refactor findBinary so that findSoftware is even easier.
	findBinary is not so much needed anymore.
	
	detectModel gegneric for all wrappers
	checkModel generic for all  wrappers

	